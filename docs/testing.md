# Testing

The pytest suite covers a valid synthetic image, bounded metrics, JSON contract shape, an unreadable file, and a missing path. Manual verification includes launching Streamlit, uploading a real image, checking all three tabs, and downloading JSON.
