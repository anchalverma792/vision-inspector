# Challenges

The main challenge was making a single score useful without hiding the reason for a warning. The implementation therefore exposes component measurements. Color clustering is kept fast and deterministic through thumbnail sampling and fixed criteria.
